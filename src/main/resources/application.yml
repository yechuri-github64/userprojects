datasources:
  default:
    url: jdbc:mysql://localhost:3306/accounts_db
    username: root
    password: root

micronaut:
  application:
    name: accounts-management-lambda
  server:
    port: 8080

logging:
  level:
    ROOT: INFO
