micronaut:
  application:
    name: test-contracts-mgmt

server:
  port: 8080

# Datasource configuration for MySQL
datasources:
  default:
    url: "jdbc:mysql://161.97.137.17:3306/orders_management"
    driverClassName: com.mysql.cj.jdbc.Driver
    username: admin_orders
    password: Test@123456!
    maximumPoolSize: 10
    dialect: MYSQL

# Each backend connection should have its own configuration section above. If you add others, add separate keys under datasources.
