datasources:
  default:
    url: jdbc:mysql://localhost:3306/accounts_db
    username: root
    password: password

micronaut:
  application:
    name: accounts-management-lambda
  server:
    port: 8080

jackson:
  serialization:
    fail-on-empty-beans: false
