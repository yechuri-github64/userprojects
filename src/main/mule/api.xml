<?xml version="1.0" encoding="UTF-8"?>
<mule
 xmlns:api-gw="http://www.mulesoft.org/schema/mule/api-gw" xmlns:http="http://www.mulesoft.org/schema/mule/http" xmlns:doc="http://www.mulesoft.org/schema/mule/documentation" xmlns="http://www.mulesoft.org/schema/mule/core" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
 http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd">

 <http:listener-config name="HTTP_Listener_config" doc:name="HTTP Listener config" host="${http.host}" port="${http.port}">
 <http:listener-connection />
 </http:listener-config>

 <flow name="api-router-flow" doc:id="api-router-flow" >
 <http:listener doc:name="Listener" config-ref="HTTP_Listener_config" path="/accounts/*" />
 <set-variable variableName="httpMethod" value="#[message.inboundProperties['http.request.method']]" />
 <choice doc:name="Route Requests">
 <when expression="#[attributes.requestPathInfo.path == '/accounts' and attributes.requestPathInfo.method == 'GET']">
 <flow-ref name="listAccountsFlow" />
 </when>
 <when expression="#[attributes.requestPathInfo.path == '/accounts' and attributes.requestPathInfo.method == 'POST']">
 <flow-ref name="createAccountsFlow" />
 </when>
 <when expression="#[attributes.requestPathInfo.path matches '/accounts/[^/]+$' and attributes.requestPathInfo.method == 'GET']">
 <flow-ref name="getAccountFlow" />
 </when>
 <when expression="#[attributes.requestPathInfo.path matches '/accounts/[^/]+$' and attributes.requestPathInfo.method == 'PUT']">
 <flow-ref name="updateAccountFlow" />
 </when>
 <when expression="#[attributes.requestPathInfo.path matches '/accounts/[^/]+$' and attributes.requestPathInfo.method == 'DELETE']">
 <flow-ref name="deleteAccountFlow" />
 </when>
 <otherwise>
 <set-payload value='{"error":"Unsupported operation"}' />
 <set-property propertyName="http.status" value="400" />
 </otherwise>
 </choice>
 </flow>


</mule>