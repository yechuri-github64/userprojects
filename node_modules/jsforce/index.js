// Minimal mock of jsforce used only for local tests when real package isn't installed.
module.exports = {
  Connection: function () {
    return {
      login: async () => {},
      sobject: function () {
        return {
          retrieve: async (id) => ({ Id: id, Name: `Account ${id}` }),
        };
      },
    };
  },
};
